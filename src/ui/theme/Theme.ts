import IRgba from '@/math/IRgba';

interface Theme {
  textColor: IRgba;
  backgroundColor: IRgba;
  marginBackgroundColor: IRgba;
  borderColor: IRgba;
  minorBorderColor: IRgba;
}

export default Theme;